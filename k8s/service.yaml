apiVersion: v1
kind: Service
metadata:
  name: landscape-frontiers
  namespace: biodiversity
  labels:
    app: landscape-frontiers
spec:
  type: ClusterIP
  selector:
    app: landscape-frontiers
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      name: http
